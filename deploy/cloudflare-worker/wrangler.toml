name = "shareguard-api-gateway"
main = "src/index.js"
compatibility_date = "2026-08-01"
workers_dev = false
preview_urls = false
routes = [
  { pattern = "api.shareguard.systems/*", zone_name = "shareguard.systems" }
]

[vars]
ALLOWED_ORIGIN = "https://shareguard.systems"
